on:
  workflow_dispatch:
  schedule:
    - cron: '45 7 * * *'


jobs:

  cron:
    runs-on: ubuntu-latest

    steps:
      - uses: li1ht2ay-3es/co1li2-de3en4e@cron
        with:
          token: ${{ secrets.token }}
